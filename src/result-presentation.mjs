// Shared by the tool response and the card's completion notification.
export const V2_RESULT_TOOL_DESCRIPTION = "Use English for Apiosk workflow messages, unless a translation is explicitly requested. Display Apiosk prices and charges in USD, never settlement-token names. Output: this tool displays the complete result in an interactive card. After it, write only a brief confirmation (entity, source, period/date and optional unit caveat) and source citation. Do not add a second table, figures, costs, missing-data list or analysis unless the user explicitly requests those details. 'Show the result' or 'retrieve annual accounts' means display the card, not write a second report.";
export const V2_RESULT_PRESENTATION = "When the interactive Apiosk result card is visible, the default reply after it is only 1–3 short sentences plus a source citation: identify what was retrieved, the entity, source, covered period and relevant source dates. A short currency/unit caveat is allowed when relevant. Do not repeat the card's figures, tables, JSON, charges, fees, balance or detailed missing-data lists. Give those details or analysis only when the actual user explicitly asks for them; a request to fetch or show records alone is not such a request. Use only returned evidence. If no result card can be displayed, provide the requested source-backed answer in text.";

export const V2_RESULT_READY_PROMPT = "The Apiosk result is now visible in the updated card. Add only a brief completion note and source citation below it. Do not generate a second detailed answer automatically. " + V2_RESULT_PRESENTATION + " Do not purchase the same data again.";
